export const TASKS_APP_SLUG = 'tasks'

export const DEFAULT_PROJECT_NAME = 'Suite Work'

/** One board = one `.tasks` package folder. */
export const TASK_PACKAGE_SUFFIX = '.tasks'

/** Files inside a `.tasks` package. */
export const TASK_PACKAGE_MANIFEST_FILE = 'manifest.json'
export const TASK_PACKAGE_CONTENT_FILE = 'tasks.json'
